import { useEffect } from 'react';
export default function MarketingSEO({ title, description, path, schema }: { title:string; description:string; path:string; schema?:Record<string,unknown> }) { useEffect(()=>{ const origin=window.location.origin; document.title=title; document.documentElement.lang='ar'; document.documentElement.dir='rtl'; const tags:[string,string,string][]=[['name','description',description],['property','og:title',title],['property','og:description',description],['property','og:type','website'],['property','og:url',origin+path],['name','twitter:card','summary_large_image'],['name','twitter:title',title],['name','twitter:description',description]]; tags.forEach(([a,b,c])=>{let el=document.head.querySelector(`meta[${a}="${b}"]`) as HTMLMetaElement|null;if(!el){el=document.createElement('meta');el.setAttribute(a,b);document.head.appendChild(el)}el.content=c}); let canonical=document.head.querySelector('link[rel="canonical"]') as HTMLLinkElement|null;if(!canonical){canonical=document.createElement('link');canonical.rel='canonical';document.head.appendChild(canonical)}canonical.href=origin+path; let script=document.getElementById('marketing-schema');if(script)script.remove();if(schema){script=document.createElement('script');script.id='marketing-schema';script.type='application/ld+json';script.textContent=JSON.stringify(schema);document.head.appendChild(script)} return()=>{document.documentElement.lang='en';document.documentElement.dir='ltr'}; },[title,description,path,schema]); return null; }
